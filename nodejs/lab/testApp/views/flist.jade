extends layout

block content
  div.container
    h1.alert= title
    ul
      each flight in flights
        - flight = flight.getInformation()
        li= flight.number + ': ' + flight.origin + '-' + flight.destination